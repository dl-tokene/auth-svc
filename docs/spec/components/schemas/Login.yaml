allOf:
  - $ref: '#/components/schemas/LoginKey'
  - type: object
    required:
      - attributes
    properties:
      attributes:
        type: object
        required:
          - auth_pair
        properties:
          auth_pair:
            $ref: '#/components/schemas/AuthPair'