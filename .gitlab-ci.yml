image: node:latest

pages:
  stage: deploy
  script:
  - npm install -g redoc-cli
  - redoc-cli bundle -o docs/web/index.html docs/spec/openapi.yaml
  artifacts:
    paths:
    - public
  only:
  - main