image: node:latest

pages:
  image: node:8
  stage: pages
  tags:
  - tokene
  only:
    refs:
    - fix/gitlab-pages
    changes:
    - docs/**/*
  script:
  - cd docs
  - npm install
  - mkdir -p ../public
  - npm run release-build
  artifacts:
    paths:
    - public
